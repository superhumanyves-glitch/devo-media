# Server-side secret used by the Vercel serverless email functions (api/*).
# Set this in the Vercel project settings (Environment Variables), NOT here.
# Never prefix it with VITE_ — that would expose it in the browser bundle.
RESEND_API_KEY=

# Optional: BCC address that also receives every lead notification.
NOTIFY_FALLBACK_EMAIL=
